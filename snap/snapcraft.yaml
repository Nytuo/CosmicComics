name: cosmic-comics # you probably want to 'snapcraft register <name>'
base: core18 # the base snap is the execution environment for this snap
version: '0.0.22' # just for humans, typically '1.2+git' or '1.3.2'
summary: Comics and Manga Reader # 79 char long summary
description: |
  This Application is a Comics and Manga reader.
  Wrote in Javascript, using Electron, NodeJS.

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil nodejs
